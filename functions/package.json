{
  "name": "@mba/functions",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "exports": {
    ".": "./lib/index.js",
    "./spark-adapter": {
      "types": "./spark-adapter.d.ts",
      "import": "./lib/spark-adapter.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run src",
    "test:rules": "vitest run src/firestore.rules.test.ts"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@mba/domain": "1.0.0",
    "@firebase/rules-unit-testing": "^5.0.1",
    "tsup": "^8.5.1",
    "typescript": "^5.7.3",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": "20"
  }
}
